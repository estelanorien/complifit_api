# Production environment variables for Google Cloud Run
# These should be set in Cloud Run Environment Variables UI
# or through cloudbuild.yaml / deployment scripts

ALLOWED_ORIGINS=capacitor://localhost,ionic://localhost,http://localhost,https://localhost,http://localhost:5173,http://localhost:3000
NODE_ENV=production
